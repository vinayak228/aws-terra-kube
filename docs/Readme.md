# This project deals with the following

Application stack (nginx, django, postgres, react, docker compose)

1. Creating AWS EC2 setup with the application deployed.
2. This see if it can be moved to ECS
3. Then deploy it on EKS setup
4. Then automate the infra using terraform

